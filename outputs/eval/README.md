# Evaluation Outputs

Stores intermediate evaluation artifacts (e.g., CoT example JSON, per-model result snapshots).

Generated automatically when running:
```bash
python scripts/benchmark_llm_prompt_strategies.py
```

Key files written here:
- `llm_cot_examples.json` — first Q&A per subject for each model under CoT strategy
