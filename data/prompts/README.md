# Prompts

Reserved for storing reusable prompt templates.

Currently empty — prompt logic is embedded directly in:
- `scripts/benchmark_llm_prompt_strategies.py` (zero-shot, few-shot, CoT builders)
- `app/llm_server.py` (system prompt for voice pipeline)
