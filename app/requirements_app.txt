# Voice AI Assistant — unified environment requirements
# Base: use your existing 'llm' conda env (already has llama-cpp-python + CUDA)
# Then pip install the extras below:

gradio>=4.0
faster-whisper>=1.0
f5-tts>=1.0          # voice cloning; auto-downloads ~1.3 GB weights on first run
soundfile
numpy
torch
# llama-cpp-python is already installed via the custom wheel at project root:
#   pip install ..\llama_cpp_python-0.3.16+cuda13.0.sm100.blackwell-cp311-cp311-win_amd64.whl
